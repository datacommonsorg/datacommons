# Formats code with ruff via `hatch fmt`
repos:
  - repo: local
    hooks:
      - id: ruff-format-hatch-settings
        name: hatch-ruff
        language: system
        entry: hatch fmt
        verbose: true
